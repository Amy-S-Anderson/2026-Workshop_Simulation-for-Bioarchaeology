% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fertility.R, R/simulate.R
\name{apply_fertility}
\alias{apply_fertility}
\title{Apply fertility to the living population for one timestep}
\usage{
apply_fertility(pop, tfr, asfr, dx = 1, pop_config)

apply_fertility(pop, tfr, asfr, dx = 1, pop_config)
}
\arguments{
\item{pop}{Population data frame}

\item{tfr}{Numeric. Total Fertility Rate.}

\item{asfr}{Named numeric vector of age-specific fertility rates, as
returned by compute_trapezoid_asfr(). The names must be character
representations of integer ages.}

\item{dx}{Numeric. timestep size. Scales fertility rates proportionally.
Default 1 (annual timestep).}

\item{pop_config}{A list of population trait parameter values to be called so that newborns have the same columns as the existing population.}

\item{cohort}{Population data frame}

\item{Alive}{Integer vector of row indices for living agents}
}
\value{
Updated cohort data frame with new agents appended

Updated pop data frame with new agents appended
}
\description{
Determines how many births occur this timestep and appends new agents to
the cohort. The number of births is drawn from a Poisson distribution whose
rate equals the sum of age-specific fertility rates across all reproductive-
age women in the living population.

Determines how many births occur this timestep and appends new agents to
the pop. The number of births is drawn from a Poisson distribution whose
rate equals the sum of age-specific fertility rates across all reproductive-
age women in the living population.
}
\details{
Women are approximated as half of all living agents aged between age_start
and age_end (inclusive). This assumption can be replaced once sex is added
as a column to the cohort data frame, by filtering on cohort$sex == "F"
instead.

Birth counts are drawn as Poisson rather than Bernoulli because multiple
births can occur at a given age in a population of any size, and the Poisson
approximation to the sum of many independent Bernoulli trials is exact in
the limit of large population and small per-individual rate.

Women are approximated as half of all living agents aged between age_start
and age_end (inclusive). This assumption can be replaced once sex is added
as a column to the pop data frame, by filtering on pop$sex == "F"
instead.

Birth counts are drawn as Poisson rather than Bernoulli because multiple
births can occur at a given age in a population of any size, and the Poisson
approximation to the sum of many independent Bernoulli trials is exact in
the limit of large population and small per-individual rate.
}
\keyword{I}
\keyword{and}
\keyword{apply_fertility}
\keyword{are}
\keyword{birth}
\keyword{call.}
\keyword{columns}
\keyword{create_pop}
\keyword{for}
\keyword{generate}
\keyword{in}
\keyword{internal}
\keyword{reference}
\keyword{same}
\keyword{sensible}
\keyword{specified}
\keyword{that}
\keyword{the}
\keyword{to}
\keyword{values}
\keyword{want}
