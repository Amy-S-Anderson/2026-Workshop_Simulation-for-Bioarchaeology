% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fertility.R, R/simulate.R
\name{compute_trapezoid_asfr}
\alias{compute_trapezoid_asfr}
\title{Compute a trapezoid age-specific fertility schedule from a TFR}
\usage{
compute_trapezoid_asfr(
  ages,
  tfr,
  age_start = 15,
  age_peak_start = 25,
  age_peak_end = 35,
  age_end = 45
)

compute_trapezoid_asfr(
  ages,
  tfr,
  age_start = 15,
  age_peak_start = 25,
  age_peak_end = 35,
  age_end = 45
)
}
\arguments{
\item{ages}{Integer vector of ages to compute the schedule over}

\item{tfr}{Numeric. Total Fertility Rate (expected lifetime births per woman)}

\item{age_start}{Numeric. Age at which fertility begins. Default 15.}

\item{age_peak_start}{Numeric. Age at which fertility reaches its peak. Default 25.}

\item{age_peak_end}{Numeric. Age at which fertility begins to decline. Default 35.}

\item{age_end}{Numeric. Age at which fertility reaches zero. Default 45.}
}
\value{
Named numeric vector of annual age-specific fertility rates

Named numeric vector of annual age-specific fertility rates
}
\description{
The schedule rises linearly from 0 at age_start to a peak at age_peak_start,
remains flat to age_peak_end, then falls linearly back to 0 at age_end.
The peak rate is derived analytically so that the sum of ASFRs across all
ages equals the TFR (i.e. the schedule integrates correctly by construction).

The schedule rises linearly from 0 at age_start to a peak at age_peak_start,
remains flat to age_peak_end, then falls linearly back to 0 at age_end.
The peak rate is derived analytically so that the sum of ASFRs across all
ages equals the TFR (i.e. the schedule integrates correctly by construction).
}
\keyword{internal}
